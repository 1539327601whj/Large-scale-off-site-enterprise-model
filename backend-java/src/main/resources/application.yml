server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enterprise_llm?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1539327601
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

# Python RAG 后端地址
python:
  backend:
    url: http://localhost:8000

# JWT 配置
jwt:
  secret: EnterpriseLLMSecretKey2026ForJWTTokenGenerationAndValidation
  expiration: 86400000  # 24小时
